image: alpine:latest

pages:
  stage: deploy
  script:
  - echo 'Creating public folder'
  - mkdir public
  - cp -r docs/* public
  artifacts:
    paths:
    - public/
  only:
  - master
  tags:
  - docker